const assert = require('assert');
const brain = require('../brain');

describe('example', () => {
  it('should do fine', () => {
    assert(true, true);
  });
});

describe('brain', () => {
	it('foo', () => {
		assert(true);
	});
});